document.addEventListener("DOMContentLoaded", function() {
    // Update footer title dynamically
    document.getElementById("footer-title").innerText = "New Footer Title";

    // Update section one title dynamically
    document.getElementById("section-one-title").innerText = "New Section One Title";

    // Update landing title dynamically
    document.getElementById("landing-title").innerText = "New Landing Title";

    // Update landing description dynamically
    document.getElementById("landing-description").innerText = "New landing description text goes here.";
});
